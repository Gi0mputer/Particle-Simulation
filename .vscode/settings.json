{
    "cmake.generator": "Ninja", // Usa Ninja come generatore predefinito
    "cmake.buildDirectory": "${workspaceFolder}/build", // Directory di build
    "cmake.configureOnOpen": true // Configura automaticamente il progetto
}
