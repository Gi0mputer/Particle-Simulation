{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Release (Ctrl+F5 per Run, F5 + task)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/bin/Release/ParticleSimulation.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "integratedTerminal",
            "preLaunchTask": "Build Release (Complete)"
        }
    ]
}
