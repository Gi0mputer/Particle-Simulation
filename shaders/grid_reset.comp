#version 450 core
layout(local_size_x = 256) in;

layout(std430, binding = 3) buffer GridHeadBuffer {
    int heads[];
} gridHead;

uniform int uNumCells;

void main() {
    uint idx = gl_GlobalInvocationID.x;
    if (idx >= uint(uNumCells)) return;
    gridHead.heads[idx] = -1;
}
